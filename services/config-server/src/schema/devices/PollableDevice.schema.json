{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://github.com/TWilkin/powerpi/tree/master/services/clacks-config/src/schema/devices/PollableDevice.schema.json",
    "title": "PollableDevice",
    "description": "The common properties for a pollable device or sensor in PowerPi",
    "type": "object",
    "properties": {
        "poll_frequency": {
            "description": "The frequency in seconds to poll this device, or disabled",
            "type": ["integer", "null"]
        }
    },
    "required": []
}
