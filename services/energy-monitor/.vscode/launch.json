{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Energy Monitor",
            "type": "go",
            "request": "launch",
            "program": "${workspaceFolder}",
            "presentation": {
                "group": "Services"
            }
        }
    ]
}
