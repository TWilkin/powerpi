[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
async-cache = "~=1.1.1"
lazy = "~=1.4"
powerpi-common = {path = "./../../common/python"}
pyharmony = "~=1.0.21"

[dev-packages]
autopep8 = "~=1.5.5"
powerpi-common-test = {editable = true, path = "./../../common/pytest"}
pylint = "~=2.7.1"
pytest-ordering = "~=0.6"

[requires]
python_version = "3"

[scripts]
start = "python -m harmony_controller"
test = "python -m pytest -v --cov=harmony_controller"
