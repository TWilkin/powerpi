[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
powerpi-common = {path = "./../../common/python"}
pyharmony = "~=1.0.21"

[dev-packages]
autopep8 = "~=1.5.5"
powerpi-common-test = {path = "./../../common/pytest"}
pylint = "~=2.7.1"

[requires]
python_version = "3"

[scripts]
start = "python -m harmony_controller"
test = "python -m pytest -v"
