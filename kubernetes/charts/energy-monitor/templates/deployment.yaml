{{- $data := dict
  "Secret" (list
    (dict
      "Name" "ihd-secret"
      "SubPath" "secret"
      "Env" (list
        (dict
          "Name" "IHD_SECRET_FILE"
        )
      )
    )
  )
}}
{{- include "powerpi.deployment" (merge (dict "Params" $data) . )}}
