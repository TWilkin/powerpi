{{- $data := dict
    "Name" "api-policy"
    "Label" .Chart.Name
    "Mosquitto" true
    "Database" true
    "IngressController" true
}}
{{- include "powerpi.network-policy" (merge (dict "Params" $data) .) -}}
