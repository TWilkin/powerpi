{{- $data := dict
  "Kind" "StatefulSet"
  "Image" "postgres"
  "PriorityClassName" "powerpi-critical"
  "RequestMemory" "50Mi"
  "Ports" (list (dict
    "Name" "postgres"
    "Port" 5432
  ))
  "NodeSelector" (list
    (dict
      "Name" "powerpi-storage"
      "Value" "true"
    )
  )
  "PersistentVolumeClaim" (dict
    "Name" "database-data"
    "Path" "/var/lib/postgresql/data/pgdata"
    "Claim" "database-volume-claim"
    "EnvName" "PGDATA"
  )
  "Secret" (list
    (dict
      "Name" "database-secret"
      "SubPath" "password"
      "Env" (list
        (dict
          "Name" "POSTGRES_DB"
          "Key" "schema"
        )
        (dict
          "Name" "POSTGRES_USER"
          "Key" "username"
        )
        (dict
          "Name" "POSTGRES_PASSWORD_FILE"
        )
      )
    )
  )
}}
{{- include "powerpi.deployment" (merge (dict "Params" $data) . )}}
