{{- $nodeSelector := ternary (list
    (dict
      "Name" "powerpi-storage"
      "Value" "true"
    )
  ) (list) .Values.global.useCluster 
}}

{{- $data := dict
  "Name" "database-backup-job"
  "Image" "postgres"
  "RequestMemory" "50Mi"
  "NodeSelector" $nodeSelector
  "Schedule" "5 1 * * *"
  "Command" (list "pg_dump")
  "Args" (list 
    "--host=database"
    "--file=`date +\"%Y-%m-%d\"`-powerpi.sql"
    "--data-only"
    "--compress=9"
  )
  "Secret" (list
    (dict
      "Name" "database-secret"
      "Env" (list
        (dict
          "Name" "PGDATABASE"
          "Key" "schema"
        )
        (dict
          "Name" "PGUSER"
          "Key" "username"
        )
        (dict
          "Name" "PGPASSWORD"
          "Key" "password"
        )
      )
    )
  )
}}

{{- include "powerpi.job" (merge (dict "Params" $data) . ) }}
