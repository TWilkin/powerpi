apiVersion: v1
kind: Service
metadata:
  name: database
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: database
  ports:
  - name: postgres
    protocol: TCP
    port: 5432
    targetPort: postgres
