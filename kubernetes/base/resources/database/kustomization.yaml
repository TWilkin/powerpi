apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ./persistent-volume-claim.yaml
- ./service.yaml
- ./stateful-set.yaml

commonLabels:
  app.kubernetes.io/name: database
  app.kubernetes.io/version: 13.0-alpine
  app.kubernetes.io/component: database

images:
- name: postgres
  newTag: 13.0-alpine
