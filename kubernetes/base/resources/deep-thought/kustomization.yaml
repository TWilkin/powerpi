apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ./deployment.yaml
- ./service.yaml

commonLabels:
  app.kubernetes.io/name: deep-thought
  app.kubernetes.io/version: 0.5.21
  app.kubernetes.io/component: api

images:
- name: twilkin/powerpi-deep-thought
  newTag: 0.5.21
