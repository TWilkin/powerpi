{{- define "powerpi.ingress-annotations" }}
    nginx.ingress.kubernetes.io/limit-rps: "10"
    nginx.ingress.kubernetes.io/proxy-body-size: "8k"
    {{- if eq .Values.global.useSSL true }}
    nginx.ingress.kubernetes.io/hsts: "true"
    nginx.ingress.kubernetes.io/hsts-max-age: "31536000"
    nginx.ingress.kubernetes.io/hsts-include-subdomains: "true"
    {{- end }}
{{- end }}

{{- define "powerpi.ingress-tls" }}
  {{- if eq .Values.global.useSSL true }}
  tls:
  - hosts:
    - {{ .Values.global.externalHostName }}
    secretName: main-cert
  {{- end }}
{{- end }}

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
  {{- include "powerpi.labels" . }}
  annotations:
    {{- include "powerpi.ingress-annotations" . }}
spec:
  ingressClassName: public
  {{- include "powerpi.ingress-tls" . }}

  rules:
  - host: {{ .Values.global.externalHostName }}
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: api
            port:
              number: 80

      {{- if eq .Values.global.voiceAssistant true }}
      - path: /webhook_alexa
        pathType: Prefix
        backend:
          service:
            name: voice-assistant
            port:
              number: 80
      {{- end }}

      - path: /
        pathType: Prefix
        backend:
          service:
            name: ui
            port:
              number: 80

{{- if eq .Values.global.useCluster true }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-ha
  {{- include "powerpi.labels" . }}
  annotations:
    {{- include "powerpi.ingress-annotations" . }}
    nginx.ingress.kubernetes.io/affinity: "cookie"
    nginx.ingress.kubernetes.io/session-cookie-name: "route"
    nginx.ingress.kubernetes.io/session-cookie-expires: "172800"
    nginx.ingress.kubernetes.io/session-cookie-max-age: "172800"
    nginx.ingress.kubernetes.io/session-cookie-samesite: "Strict"
    nginx.ingress.kubernetes.io/session-cookie-httponly: "true"
    {{- if eq .Values.global.useSSL true }}
    nginx.ingress.kubernetes.io/session-cookie-secure: "true"
    {{- end }}
spec:
  ingressClassName: public
  {{- include "powerpi.ingress-tls" . }}

  rules:
  - host: {{ .Values.global.externalHostName }}
    http:
      paths:
      - path: /api/auth
        pathType: Prefix
        backend:
          service:
            name: api
            port:
              number: 80

      - path: /api/socket.io
        pathType: Prefix
        backend:
          service:
            name: api
            port:
              number: 80
{{- end }}
